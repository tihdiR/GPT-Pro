ChatGPT Clone with the ability to have multiple chats and choose which OpenAI engine is being used.

(API key has expired so messaging doesn't work)

Deployed on Azure:
https://gpt-pro.azurewebsites.net/
